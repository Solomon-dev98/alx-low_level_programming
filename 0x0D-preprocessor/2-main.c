#include <stdio.h>

/**
 * main - a function that prints name of its file.
 * Return: returns 0 on success
 */
int main(void)
{
	/*Prints name of the file*/

	printf("%s\n", __FILE__);
	return (0);
}
