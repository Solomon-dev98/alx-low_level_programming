#include "main.h"
#include <stdio.h>

/**
 * add - the function that adds two integers and the result
 * @i: is a parameter
 * @j: is a parameter
 * Return: always 0
 */

int add(int i, int j)
{
	return (i + j);
}
